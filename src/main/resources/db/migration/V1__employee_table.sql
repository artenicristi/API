CREATE TABLE employee(
    id INT,
    first_name VARCHAR(50),
    last_name VARCHAR(50),
    email VARCHAR(50),
    birth_date VARCHAR(50),
    salary INT,
    manager_id INT,
    company_id INT,
    PRIMARY KEY (id)
);